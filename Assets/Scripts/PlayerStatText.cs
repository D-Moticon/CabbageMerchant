using UnityEngine;

public class PlayerStatText : MonoBehaviour
{
    public enum PlayerStatType
    {
        coins,
        shop
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
